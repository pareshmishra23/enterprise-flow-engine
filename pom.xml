<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.0</version>
        <relativePath/>
    </parent>

    <groupId>com.efe</groupId>
    <artifactId>enterprise-flow-engine</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>enterprise-flow-engine</name>
    <description>Enterprise Flow Engine - reusable EFE Platform + reference reconciliation implementation</description>

    <modules>
        <module>efe-platform</module>
        <module>examples/platform-demo</module>
        <module>examples/reconciliation-example</module>
    </modules>

    <properties>
        <java.version>21</java.version>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <jackson.version>2.21.4</jackson.version>
        <efe.version>1.0.0-SNAPSHOT</efe.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.efe</groupId>
                <artifactId>efe-platform</artifactId>
                <version>${efe.version}</version>
            </dependency>
            <dependency>
                <groupId>com.efe</groupId>
                <artifactId>efe-reconciliation</artifactId>
                <version>${efe.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.13.0</version>
                    <configuration>
                        <source>21</source>
                        <target>21</target>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
