# EFE-013 — AMQ / JMS Plugin

## 1. Objective
Jakarta JMS 3.x provider for Apache ActiveMQ / Artemis, transactional sessions, and XA distributed transaction support.

## 2. Key Components
- `JmsProducerAdapter` (Connection pool, session transactions).
- `JmsConsumerAdapter` (Message listener container, client acknowledgment).

## 3. Status
- **State**: `QUEUED`
