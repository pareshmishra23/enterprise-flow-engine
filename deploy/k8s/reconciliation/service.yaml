apiVersion: v1
kind: Service
metadata:
  name: efe-reconciliation
  namespace: default
  labels:
    app: efe-reconciliation
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: efe-reconciliation
